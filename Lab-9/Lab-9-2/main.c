#include <stdio.h>
#include <locale.h>
#include <math.h>

int fst(double num)
{
    
    }

int main(void)
{

    long int n;

    scanf("%d", &n);

    int result = fst(fabs(n));

    return 0;
    }